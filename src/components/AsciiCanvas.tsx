// ASCII canvas: animated character matrix with three states — REPOSO (idle), PROCESANDO (tool/tts in-flight), HABLANDO (audio playing) — driven by audioLevel

export default function AsciiCanvas() {
  return null
}
